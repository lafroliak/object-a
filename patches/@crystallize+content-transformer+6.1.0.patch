diff --git a/node_modules/@crystallize/content-transformer/react.js b/node_modules/@crystallize/content-transformer/react.js
index 4076b4d..dd7fdaf 100644
--- a/node_modules/@crystallize/content-transformer/react.js
+++ b/node_modules/@crystallize/content-transformer/react.js
@@ -146,7 +146,7 @@ var Transformer = /*#__PURE__*/function (_React$Component) {
     });
     (0, _defineProperty2.default)((0, _assertThisInitialized2.default)(_this), "renderNodeChild", function (c, i) {
       return /*#__PURE__*/React.createElement(Transformer, (0, _extends2.default)({
-        key: i
+        key: _this.uuid(i)
       }, c, {
         overrides: _this.overrides,
         renderNode: _this.renderNode
@@ -156,6 +156,16 @@ var Transformer = /*#__PURE__*/function (_React$Component) {
   }
 
   (0, _createClass2.default)(Transformer, [{
+    key: "uuid",
+    value: function uuid(seed) {
+      var uuid = 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {
+        var r = (seed + Math.random() * 16) % 16 | 0;
+        seed = Math.floor(seed / 16);
+        return (c === 'x' ? r : r & (0x3 | 0x8)).toString(16);
+      });
+      return uuid;
+    }
+  }, {
     key: "render",
     value: function render() {
       var _this$props = this.props,
diff --git a/node_modules/@crystallize/content-transformer/src/react.jsx b/node_modules/@crystallize/content-transformer/src/react.jsx
index 1a3d257..fafb27e 100644
--- a/node_modules/@crystallize/content-transformer/src/react.jsx
+++ b/node_modules/@crystallize/content-transformer/src/react.jsx
@@ -60,9 +60,19 @@ class Transformer extends React.Component {
     });
   };
 
+  uuid(seed) {
+    const uuid = 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {
+      const r = (seed + Math.random() * 16) % 16 | 0;
+      seed = Math.floor(seed/16);
+
+      return (c === 'x' ? r : r & (0x3|0x8)).toString(16);
+    });
+    return uuid;
+  }
+
   renderNodeChild = (c, i) => (
     <Transformer
-      key={i}
+      key={this.uuid(i)}
       {...c}
       overrides={this.overrides}
       renderNode={this.renderNode}
